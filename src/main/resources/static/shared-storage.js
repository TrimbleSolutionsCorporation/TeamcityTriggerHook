/**
 * A module for storing and sharing data across closure templating framework and RequireJS framework.
 *
 * @returns {object} Empty object for data sharing
 */

define('com/trimble/tekla/teamcity/shared-storage', {});
